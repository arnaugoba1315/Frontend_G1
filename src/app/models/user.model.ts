export interface User {
    id: number;
    username: string;
    bio: string;
    profilePicture: string;
    level: number;
    email: string;
  }
export class User implements User {
    constructor(     
      
    ) {}
}
  